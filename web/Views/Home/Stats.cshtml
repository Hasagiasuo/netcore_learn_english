@model string[]
@{
    ViewData["Title"] = "Animals";
}

<<<<<<< HEAD
<h2>Word Learning Stats</h2>
<p>Total Words: @ViewBag.TotalWords</p> <!-- Виведення загальної кількості слів -->
<!-- Canvas для діаграми -->
<div style="width: 300px; height: 300px;">
    <canvas id="myPieChart"></canvas>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
    var ctx = document.getElementById('myPieChart').getContext('2d');
    var myPieChart = new Chart(ctx, {
        type: 'pie',
        data: {
            labels: ['Learned Words', 'Remaining Words'],
            datasets: [{
                data: [@ViewBag.LearnedPercentage, @ViewBag.RemainingPercentage],
                backgroundColor: ['#33FF57', '#FF5733'], // Зелений для вивчених, червоний для невивчених
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false, // Вимикаємо збереження пропорцій
            plugins: {
                legend: {
                    position: 'top',
                },
                tooltip: {
                    callbacks: {
                        label: function (tooltipItem) {
                            return tooltipItem.label + ': ' + tooltipItem.raw + '%';
                        }
                    }
                }
            }
=======
<!DOCTYPE html>
<html>
<head>
    <title>@ViewData["Title"]</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f4f4f4;
>>>>>>> 831954173e2f7625e8e017d40708f5ce2854a136
        }

        h2 {
            color: #333;
            border-bottom: 2px solid #333;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            background-color: #fff;
            border: 1px solid #ddd;
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 10px;
            font-size: 16px;
            line-height: 1.5;
        }

        form {
            margin-top: 20px;
        }

        input[type="text"] {
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ddd;
            border-radius: 5px;
            margin-right: 10px;
        }

        button {
            padding: 10px 15px;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            color: #fff;
            background-color: #333;
            cursor: pointer;
        }

        button:hover {
            background-color: #555;
        }
    </style>
</head>
<body>
    <h2>@ViewData["Title"]</h2>

    <ul>
        @foreach (var item in Model)
        {
            <li>@item</li>
        }
    </ul>

   @*  <form method="post" asp-action="AddAnimal">
        <input type="text" name="newAnimal" placeholder="Enter new animals" />
        <button type="submit">Add Animal</button>
    </form>

    <form method="post" asp-action="DeleteAnimal">
        <input type="text" name="animalToDelete" placeholder="Enter animals to delete" />
        <button type="submit">Delete Animals</button>
    </form> *@
</body>
</html>
